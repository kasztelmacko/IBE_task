# IBE Task
To replicate the code:

1. Create a Python environment (e.g. `python -m venv venv`).
2. Activate it (`venv\Scripts\activate` on Windows, `source venv/bin/activate` on Linux/macOS).
3. Install dependencies: `pip install -r requirements.txt`.

Then inside the python notebook select the enviroment kernel 